services:
  fold:
    build: .
#    image: ognerezov/fold
    container_name: fold
    environment:
      - INPUT_DIR=/service
      - INPUT_INIT=true
    ports:
      - '3333:3333'
    volumes:
      - /Users/ognerezov/pro/fold-data/init:/service:rw
